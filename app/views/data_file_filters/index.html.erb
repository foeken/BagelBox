<% content_for(:header) do %>
  What should we look for?
<% end %>

<h2>Manually added filters</h2>

<p>
  These filters have been added by you. You can have both positive filters (stuff you want) and negative filters (stuff you don't want).
</p>

<table class="data_file_filter_table" CELLSPACING="0">
  <tr>
    <th>&nbsp</th>
    <th>Name</th>
  </tr>

<% @data_file_filters.select{ |x| x.source_id.nil? }.each do |data_file_filter| %>
  <tr  class="<%= cycle('even','odd') %>">
    <td width="20" style="text-align:center"><%= data_file_filter.negative ? image_tag("offline.png") : image_tag("online.png") %></td>
    <td width="500"><%=h data_file_filter.name %></td>
    <td><%= link_to 'Show', data_file_filter %></td>
    <td><%= link_to 'Edit', edit_data_file_filter_path(data_file_filter) %></td>
    <td width="50" style="text-align:right; padding-right: 10px;"><%= link_to 'Remove', data_file_filter, :confirm => 'Are you sure?', :method => :delete, :class=>'remove_link' %></td>
  </tr>
<% end %>
</table>

<br/>

<p style="text-align: right;">
<div class="button right"><%= link_to 'Add a new filter', new_data_file_filter_path %></div>
</p>

<% if @data_file_filters.reject{ |x| x.source_id.nil? }.length > 0 %>

<br/>

<h2>Filters generated by a filter source</h2>

<p>
  These filters have been automatically generated by a Filter source.
</p>

<table class="data_file_filter_table" CELLSPACING="0">
  <tr>
    <th>&nbsp</th>
    <th>Name</th>
  </tr>

<% @data_file_filters.reject{ |x| x.source_id.nil? }.each do |data_file_filter| %>
  <tr  class="<%= cycle('even','odd') %>">
    <td width="20" style="text-align:center"><%= data_file_filter.negative ? image_tag("offline.png") : image_tag("online.png") %></td>
    <td width="500"><%=h data_file_filter.name %></td>
    <td><%= link_to 'Show', data_file_filter %></td>
    <td><%= link_to 'Edit', edit_data_file_filter_path(data_file_filter) %></td>
    <td width="50" style="text-align:right; padding-right: 10px;"><%= link_to 'Remove', data_file_filter, :confirm => 'Are you sure?', :method => :delete, :class=>'remove_link' %></td>
  </tr>
<% end %>
</table>

<% end %>