<% content_for(:header) do %>
  What goes where?
<% end %>

<%= render :partial => "sub_menu" -%>

<h2>Advanced setup</h2>

<table class="settings_table">
  <tr>
    <th>Description</th>
    <th>Value</th>
  </tr>

<% @settings.each do |setting| %>
  <tr class="<%= cycle('even','odd') %>">
    <td><%=h setting.description %></td>
    <td><%=h setting.value %></td>
    <td><%= link_to 'Edit', edit_setting_path(setting) %></td>
    
    <% if RAILS_ENV == 'development' %>
    <td width="50" style="text-align:right; padding-right: 10px;"><%= link_to 'Remove', setting, :confirm => 'Are you sure?', :method => :delete, :class => 'remove_link' %></td>
    <% end %>
  </tr>
<% end %>
</table>

<% if RAILS_ENV == 'development' %>
<br />
<p style="text-align:right">
  <div class="button right"><%= link_to 'Add a new setting', new_setting_path %></div><br/>
</p>
<% end %>